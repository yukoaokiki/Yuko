<div class="tweets-container">
<section id="first-block">
 <div class="title-container">

   <div class="title">

    <h1><span style="color:#C3DF5F;">01</span> 東京</h1>
    <h2>Tokyo</h1>

   </div>
  </div>
</section>


<% @tweets.each do |t| %>
    <% if t.category == "tokyo" %>  
    <p>内容:<%= t.name %></p>
    <%= image_tag t.image_url, size: "250x200" if t.image? %>

    <% end %>
<% end %>
